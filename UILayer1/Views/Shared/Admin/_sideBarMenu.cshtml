<div class="" style="padding-top:60px;">
    <div class="sidebar">
        <a class="" asp-action="">Dashboard</a>
        <a id="product" class="@(Context.Request.Path == "/" ? "active" : "")">Products</a>
        <div id="dropdown">
            <a asp-action="Create" class="@(Context.Request.Path == "/Product/Create" ? "active " : "")">Create</a>
            <a asp-action="Edit" class="@(Context.Request.Path == "/Product/Edit" ? "active " : "")">Edit</a>
        </div>
        <a asp-action="">Settings</a>

        <a asp-action="Create">Users</a>

        <a id="orders">Orders</a>
    </div>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script>
        $(document).ready(function(){
  $("#product").click(function(){
    $("#dropdown").toggle();
  });
});
    </script>
    <style>
        #dropdown {
            display: none;
            background-color: #262626;
            padding-left: 8px;
        }
    </style>
